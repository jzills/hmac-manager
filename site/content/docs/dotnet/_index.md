---
title: .NET library
linkTitle: .NET
description: Registration, authorization, events, dynamic policies and logging for ASP.NET Core.
weight: 3
---

The `HmacManager` NuGet package. It signs outgoing requests, verifies incoming
ones, or both, and targets `net8.0` and `net10.0`.

```bash
dotnet add package HmacManager
```

{{< hm-children >}}
